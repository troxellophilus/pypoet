"""Run 'python setup.py install' to install pypoet package."""

from setuptools import setup

setup(name="PyPoet",
      version="0.1.0",
      description="Python that writes Python.",
      url="",
      author="Drew Troxell",
      author_email="drew.troxell@rosetta.com",
      license="Other/Proprietary License",
      packages=["pypoet"],
      install_requires=[],
      include_package_data=True)
